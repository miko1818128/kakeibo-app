const SHEET_NAME='アプリ同期';
function sheet_(){return SpreadsheetApp.getActiveSpreadsheet().getSheetByName(SHEET_NAME)||SpreadsheetApp.getActiveSpreadsheet().insertSheet(SHEET_NAME)}
function doGet(e){const sh=sheet_(),v=sh.getDataRange().getValues();let records=[],config=null;for(let i=1;i<v.length;i++){if(v[i][0]==='RECORD')try{records.push(JSON.parse(v[i][1]))}catch(_){}if(v[i][0]==='CONFIG')try{config=JSON.parse(v[i][1])}catch(_){}}const out=JSON.stringify({ok:true,records,config}),cb=e.parameter.callback;return ContentService.createTextOutput(cb?cb+'('+out+')':out).setMimeType(cb?ContentService.MimeType.JAVASCRIPT:ContentService.MimeType.JSON)}
function doPost(e){const p=JSON.parse(e.postData.contents),sh=sheet_(),old=sh.getDataRange().getValues(),map=new Map();for(let i=1;i<old.length;i++)if(old[i][0]==='RECORD')try{const r=JSON.parse(old[i][1]);map.set(r.id,{r,row:i+1})}catch(_){}for(const r of(p.records||[])){if(!r.id)continue;const o=map.get(r.id);if(!o)sh.appendRow(['RECORD',JSON.stringify(r),r.updatedAt||Date.now()]);else if((r.updatedAt||0)>(o.r.updatedAt||0))sh.getRange(o.row,2,1,2).setValues([[JSON.stringify(r),r.updatedAt||Date.now()]])}const c=p.config||{},data=sh.getDataRange().getValues();let row=-1;for(let i=1;i<data.length;i++)if(data[i][0]==='CONFIG'){row=i+1;break}if(row<0)sh.appendRow(['CONFIG',JSON.stringify(c),c.updatedAt||Date.now()]);else{let oc={};try{oc=JSON.parse(data[row-1][1])}catch(_){}if((c.updatedAt||0)>(oc.updatedAt||0))sh.getRange(row,2,1,2).setValues([[JSON.stringify(c),c.updatedAt||Date.now()]])}return ContentService.createTextOutput(JSON.stringify({ok:true})).setMimeType(ContentService.MimeType.JSON)}
function setup(){const sh=sheet_();if(sh.getLastRow()===0)sh.appendRow(['TYPE','JSON','UPDATED_AT'])}